{
  "name": "@fasm2-studio/server",
  "private": true,
  "version": "0.1.0",
  "description": "Language Server for FASM2 (fasmg) and FASM1",
  "license": "MIT",
  "main": "dist/server.js",
  "scripts": {
    "build": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "typecheck": "tsc -p . --noEmit",
    "test": "mocha --require tsx 'test/**/*.test.ts'"
  },
  "dependencies": {
    "vscode-languageserver": "^10.1.0",
    "vscode-languageserver-textdocument": "^1.0.12",
    "vscode-uri": "^3.1.0"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^26.1.1",
    "esbuild": "^0.28.1",
    "mocha": "^11.7.6",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3"
  }
}
